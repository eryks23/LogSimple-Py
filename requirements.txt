# LogSimple-Py has no third-party dependencies.
# It relies only on the Python standard library: re, logging, typing.
# This file is included for tooling/CI compatibility (e.g. `pip install -r requirements.txt`).
